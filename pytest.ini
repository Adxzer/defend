[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast, isolated unit tests without external services
    integration: tests involving multiple components or services
    api: HTTP-level API tests hitting a running server
    perf: performance smoke tests